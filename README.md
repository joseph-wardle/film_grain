# Physically Based Film Grain Rendering

Realistic film grain rendering, with **CPU** and **GPU (WGPU)** backends, implementing the **pixel-wise** and **grain-wise** algorithms from Newson et al. (2017).

